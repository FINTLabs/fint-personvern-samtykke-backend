spring:
  webflux:
    base-path: /api/samtykke
  profiles:
    include:
      - kafka
      - oauth

springdoc:
  swagger-ui:
    path: /swagger

logging:
  level:
    org.apache.kafka: OFF
    no.fintlabs.kafka: OFF
    org.springframework.kafka: OFF
    no.fintlabs: DEBUG